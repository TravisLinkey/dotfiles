---
description: Implementation phase
agent: build
---

Read the files in the `CONTEXT` folder paying special attention to the `00__PLAN_FILE.txt` and the `## Implementation Steps` and `## Relevant code snippets` sections

Begin the implementation and be sure to write back your changes into the `CONTEXT/00__PLAN_FILE.txt` once you are done. Including any `## Remaining Steps`
